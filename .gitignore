# --- Hydra-Worm Research Project Gitignore ---

# 1. Rust Artifacts (hydra-agent)
**/target/
**/*.rs.bk
# Cargo.lock is usually kept for binaries to ensure build reproducibility
# Keep it if you want consistent builds, ignore it if you want latest versions:
# Cargo.lock 

# 2. Go Artifacts (hydra-c2 / orchestrator)
# Ignore specific binary names and common Go output patterns
**/hydra-c2
**/hydra-agent
**/hydra-c2-bin
**/hydra-orchestrator
**/*.exe
**/*.test
**/*.out
**/hydra.tmp

# 3. Docker & Lab Environment
.dockerbin/
**/compose.override.yml
# Ignore BuildKit metadata if generated locally
.buildx/

# 4. Development & OS Garbage
.DS_Store
.vscode/
.idea/
*.log
*.tmp
# Makefile local vars
.env

# 5. Security Research Specifics (Operational Security)
/artifacts/
/loot/
/captures/*.pcap
passwords.txt
targets.json