# --- Hydra-Worm Research Project Gitignore ---

# 1. Rust Artifacts (hydra-agent)
/hydra-agent/target/
/hydra-agent/Cargo.lock
hydra-agent/target/debug/hydra-agent
**/*.rs.bk
Cargo.lock

# 2. Go Artifacts (hydra-c2)
/hydra-c2/hydra-c2       # The compiled binary
/hydra-c2/hydra-c2-bin   # Alternative binary names
/hydra-c2/go.sum         # Optional: Some keep this, but for R&D we can ignore if deps change fast
# /hydra-c2/.env         # Secret environment variables

# 3. Development & OS Garbage
.DS_Store
.vscode/
.idea/
*.log
*.tmp

# 4. Security Research Specifics
# Ignore any harvested artifacts during testing
/artifacts/
/loot/
passwords.txt
targets.json